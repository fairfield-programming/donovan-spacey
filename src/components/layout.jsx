module.exports = function Layout({ children }) {

    return <div>
        <header></header>
        <main>
            {children}
        </main>
        <footer>Made with ❤️ through Donovan</footer>
    </div>

}